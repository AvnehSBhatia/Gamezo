# Eazo Private Key to decrypt the user session token on the server
EAZO_PRIVATE_KEY=your_eazo_private_key_here

# Eazo app ID. Read by RootLayout and passed into <EazoProvider> as a prop.
EAZO_APP_ID=your_eazo_app_id_here

# --- Deployment (public) ---
# Canonical public URL for OG tags / share links (custom domain recommended).
# NEXT_PUBLIC_APP_URL=https://gamezo.example.com

# --- Vercel (zero setup) ---
# Deploy to Vercel — matchmaking runs in-memory via HTTP polling. No database or game server needed.
# Optional: set APP_URL to your production URL for share links / AI judge callbacks.

# --- Local dev ---
# npm run dev — starts game server + Next.js with WebSocket proxy.

# --- Optional overrides ---
# GAME_SERVER_HTTP= — external game server (skip built-in polling)
# DATABASE_URL= — use Postgres polling instead of in-memory (not required)

# Where the game server calls back for AI judging (defaults to APP_URL/api/ai-judge).
# APP_URL=https://gamezo.example.com

# PostgreSQL connection string (used by Drizzle ORM)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:password@localhost:5432/myapp

# Shared secret Vercel Cron uses to authenticate scheduled invocations of
# `/api/notifications/cron/daily-digest`. Generate any high-entropy string
# (e.g. `openssl rand -hex 32`) and set it both here and in Vercel's project
# env. Vercel automatically injects `Authorization: Bearer ${CRON_SECRET}`
# when firing crons declared in `vercel.json`.
CRON_SECRET=replace_with_a_long_random_string
